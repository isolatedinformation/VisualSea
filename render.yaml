services:
  - type: web
    name: seaborn-canvas
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.seaborn_canvas.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9
